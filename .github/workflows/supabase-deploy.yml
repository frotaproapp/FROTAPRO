name: Deploy FROTAPRO to Supabase

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clonar repositório
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Instalar dependências
      - name: Install dependencies
        run: npm ci

      # 3️⃣ Deploy do banco Supabase
      - name: Deploy database
        run: npx supabase db push --yes
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      # 4️⃣ Deploy das funções (opcional)
      - name: Deploy Functions
        run: |
          npx supabase functions deploy my-function --no-verify-jwt
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      # 5️⃣ Mensagem de sucesso
      - name: Success message
        run: echo "Deploy FROTAPRO concluído com sucesso! 🚀"
